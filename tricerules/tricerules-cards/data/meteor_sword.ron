// Oracle and rulings checked 2026-09-22 against pinned Scryfall oracle 181965fb-e685-4b1f-9bd6-3a33bed933b2.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and packet evidence live under authoring/review-maps and build/direct-ron.
// CR 603.6a (entry trigger), CR 701.8 (destroy), CR 301.5/702.6 (Equipment and equip), and
// CR 613.4c (attached P/T modification).
(
  id: "meteor_sword",
  name: "Meteor Sword",
  face_id: "meteor_sword",
  mana_cost: "{7}",
  types: ["Artifact", "Equipment"],
  triggered_abilities: [(
    ability_id: "triggered_01",
    presentation: OracleLines([1]),
    trigger: WhenSelfEntersBattlefield,
    effect: [Destroy(subject: Chosen((kind: AnyPermanent)))],
    targeting: Some((groups: [(
      min: 1,
      max: 1,
      prompt: "Choose target permanent",
      effect_indices: [0],
    )])),
  )],
  static_abilities: [(
    ability_id: "static_01",
    presentation: OracleLines([2]),
    definition: AttachedModifier(delta_power: 3, delta_toughness: 3),
  )],
  activated_abilities: [(
    ability_id: "activated_01",
    presentation: OracleLines([3]),
    costs: [Mana("{3}")],
    effect: [Equip(target: (kind: Creature, controller: You))],
  )],
)
