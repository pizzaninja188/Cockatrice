// Oracle and rulings checked 2026-09-21 against pinned Scryfall oracle b37e9bc5-6a2c-4f08-b856-3f8d0d16defb.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and packet evidence live under authoring/review-maps and build/direct-ron.
// Raid is an intervening-if on the controller having attacked this turn (CR 603.4).
(
  id: "fire_nation_raider",
  name: "Fire Nation Raider",
  face_id: "fire_nation_raider",
  mana_cost: "{3}{R}",
  types: ["Creature", "Human", "Soldier"],
  power: 4,
  toughness: 2,
  triggered_abilities: [(
    ability_id: "triggered_01",
    presentation: OracleLines([1]),
    trigger: WhenSelfEntersBattlefield,
    effect: [CreateTokens(token: "clue", count: 1)],
    intervening_if: Some(AttackedThisTurn(players: Controller)),
  )],
)
