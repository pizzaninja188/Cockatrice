// Oracle and rulings checked 2026-09-21 against pinned Scryfall oracle 0f1ada92-538b-40cb-9bf3-5c415f5325e6.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and packet evidence live under authoring/review-maps and build/direct-ron.
// CR 603.6a (entry trigger), CR 701.22 (scry), and CR 702.122 (crew). Crew adds the
// creature card type until end of turn without changing the Vehicle's base characteristics.
(
  id: "spotcycle_scouter",
  name: "Spotcycle Scouter",
  face_id: "spotcycle_scouter",
  mana_cost: "{1}{W}",
  types: ["Artifact", "Vehicle"],
  power: 3,
  toughness: 2,
  triggered_abilities: [(
    ability_id: "triggered_01",
    presentation: OracleLines([1]),
    trigger: WhenSelfEntersBattlefield,
    effect: [Scry(count: 2)],
  )],
  activated_abilities: [(
    ability_id: "activated_01",
    presentation: OracleLines([2]),
    costs: [TapPermanents(
      constraint: AggregateMinimum(minimum: 1, contribution: CurrentPower),
      filter: (kind: Creature, controller: You),
      exclude_source: true,
    )],
    effect: [AddTypes(subject: Source, addition: (card_types: [Creature]))],
  )],
)
