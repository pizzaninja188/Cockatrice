(
  id: "opt",
  name: "Opt",
  face_id: "opt",
  mana_cost: "{U}",
  types: ["Instant"],
  // Scry suspends for the player's decision and the following draw runs on resume
  // (PendingResolution::resume_effect_index), so the drawn card reflects the scry.
  spell_effect: [
    Scry(count: 1),
    Draw(count: 1),
  ],
)
