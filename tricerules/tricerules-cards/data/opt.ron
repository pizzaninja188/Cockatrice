(
  id: "opt",
  name: "Opt",
  mana_cost: "{U}",
  types: ["Instant"],
  // "Scry 1. Draw a card." Scry suspends for the player's decision and the draw runs on resume
  // (PendingResolution::resume_effect_index), so the drawn card reflects the scry.
  spell_effect: [
    Scry(count: 1),
    Draw(count: 1),
  ],
)
