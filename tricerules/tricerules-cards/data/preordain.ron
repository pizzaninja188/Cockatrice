(
  id: "preordain",
  name: "Preordain",
  face_id: "preordain",
  mana_cost: "{U}",
  types: ["Sorcery"],
  // Keeping both cards on top during the two-card scry prompts a second, ordering
  // choice (CR 701.18a) before the draw resumes.
  spell_effect: [
    Scry(count: 2),
    Draw(count: 1),
  ],
)
