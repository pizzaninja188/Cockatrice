// Oracle and rulings checked 2026-09-22 against pinned Scryfall oracle bd4151cc-743a-40f0-9e51-1f67555d9ab3.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and packet evidence live under authoring/review-maps and build/direct-ron.
// CR 301.5/702.6 (Equipment and equip), CR 603.6a with CR 120.3 (the source deals the entry
// damage), and CR 613.4c (attached P/T modification).
(
  id: "lion_heart",
  name: "Lion Heart",
  face_id: "lion_heart",
  mana_cost: "{4}",
  types: ["Artifact", "Equipment"],
  triggered_abilities: [(
    ability_id: "triggered_01",
    presentation: OracleLines([1]),
    trigger: WhenSelfEntersBattlefield,
    effect: [DamageTarget(amount: 2, target: (kind: AnyTarget))],
    targeting: Some((groups: [(
      min: 1,
      max: 1,
      prompt: "Choose any target",
      effect_indices: [0],
    )])),
  )],
  static_abilities: [(
    ability_id: "static_01",
    presentation: OracleLines([2]),
    definition: AttachedModifier(delta_power: 2, delta_toughness: 1),
  )],
  activated_abilities: [(
    ability_id: "activated_01",
    presentation: OracleLines([3]),
    costs: [Mana("{2}")],
    effect: [Equip(target: (kind: Creature, controller: You))],
  )],
)
