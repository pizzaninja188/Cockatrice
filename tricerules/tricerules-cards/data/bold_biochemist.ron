// Oracle and rulings checked 2026-09-21 against pinned Scryfall oracle 45ddfa35-7b3c-4916-b231-dc721b7b56d9.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and packet evidence live under authoring/review-maps and build/direct-ron.
// Power-up: the ability can be activated only once per object and costs its mana cost less when
// the source entered this turn.
(
  id: "bold_biochemist",
  name: "Bold Biochemist",
  face_id: "bold_biochemist",
  mana_cost: "{1}{U}",
  types: ["Creature", "Human", "Scientist"],
  power: 1,
  toughness: 3,
  activated_abilities: [(
    ability_id: "activated_01",
    presentation: OracleLines([1]),
    costs: [Mana("{5}{U}")],
    cost_modifiers: [ConditionalSourceManaCostReduction(condition: PermanentsEnteredThisTurn(
      controllers: All,
      filter: (source_only: true),
      min: Some(1),
    ))],
    effect: [
      PutCounters(counter: PlusOnePlusOne, count: 1, subject: Source),
      Draw(count: 2),
    ],
    activation_limit: Some(PerObject(max_activations: 1)),
  )],
)
