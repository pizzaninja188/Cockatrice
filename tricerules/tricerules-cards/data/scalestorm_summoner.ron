// Oracle and rulings checked 2026-09-21 against pinned Scryfall oracle 55ede1d4-593d-433c-943a-4db12eeb5fa8.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and packet evidence live under authoring/review-maps and build/direct-ron.
(
  id: "scalestorm_summoner",
  name: "Scalestorm Summoner",
  face_id: "scalestorm_summoner",
  mana_cost: "{2}{R}",
  types: ["Creature", "Human", "Warlock"],
  power: 3,
  toughness: 3,
  triggered_abilities: [(
    ability_id: "triggered_01",
    presentation: OracleLines([1]),
    trigger: WheneverSelfAttacks(minimum_other_attackers: 0),
    effect: [ChooseResolutionBranch(
      selection: FirstApplicable,
      branches: [
        (
          branch_id: "create_dinosaur",
          presentation: Fallback,
          cost: None,
          requirement: GameCondition(BattlefieldAggregate(
            filter: (controllers: Controller, card_type: Some(Creature)),
            aggregate: MaximumPower,
            min: Some(4),
          )),
          effects: [CreateTokens(token: "dinosaur_r_3_1", count: 1)],
        ),
        (branch_id: "no_dinosaur", presentation: Fallback, cost: None, requirement: Always, effects: []),
      ],
    )],
  )],
)
