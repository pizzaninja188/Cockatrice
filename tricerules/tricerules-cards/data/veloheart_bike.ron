// Oracle and rulings checked 2026-09-21 against pinned Scryfall oracle fa123b21-518a-4f1d-9629-c44576d45f21.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and packet evidence live under authoring/review-maps and build/direct-ron.
// CR 603.6a (entry trigger), CR 605/106.1b (any-color mana ability), and CR 702.122 (crew).
// Crew adds only the creature type until end of turn.
(
  id: "veloheart_bike",
  name: "Veloheart Bike",
  face_id: "veloheart_bike",
  mana_cost: "{2}{G}",
  types: ["Artifact", "Vehicle"],
  power: 4,
  toughness: 2,
  triggered_abilities: [(
    ability_id: "triggered_01",
    presentation: OracleLines([1]),
    trigger: WhenSelfEntersBattlefield,
    effect: [GainLife(amount: 2)],
  )],
  activated_abilities: [
    (
      ability_id: "activated_01",
      presentation: OracleLines([2]),
      costs: [Tap],
      effect: [ProduceMana(options: [(w: 1), (u: 1), (b: 1), (r: 1), (g: 1)])],
    ),
    (
      ability_id: "activated_02",
      presentation: OracleLines([3]),
      costs: [TapPermanents(
        constraint: AggregateMinimum(minimum: 2, contribution: CurrentPower),
        filter: (kind: Creature, controller: You),
        exclude_source: true,
      )],
      effect: [AddTypes(subject: Source, addition: (card_types: [Creature]))],
    ),
  ],
)
