// Oracle and rulings checked 2026-09-21 against pinned Scryfall oracle 22d0a848-2126-48f0-9050-38daaf93b1d0.
// Handwritten typed definition promoted through the reviewed direct-RON path;
// review map and review packet evidence live under authoring/review-maps and build/direct-ron.
// CR 614.1c/d: the entered-tapped replacement is evaluated against the proposed entry, so
// lands entering at the same time do not count as basic lands already controlled. CR 121.1
// (draw) and CR 701.8 (discard) for the loot ability.
(
  id: "agna_qela",
  name: "Agna Qel'a",
  face_id: "agna_qela",
  mana_cost: "",
  types: ["Land"],
  static_abilities: [(
    ability_id: "static_01",
    presentation: OracleLines([1]),
    definition: EntersTapped(
      affected: Self_,
      condition: Some(BattlefieldAggregate(
        filter: (controllers: Controller, card_type: Some(BasicLand)),
        aggregate: Count,
        max: Some(0),
      )),
    ),
  )],
  activated_abilities: [
    (
      ability_id: "activated_01",
      presentation: OracleLines([2]),
      costs: [Tap],
      effect: [ProduceMana(options: [(u: 1)])],
    ),
    (
      ability_id: "activated_02",
      presentation: OracleLines([3]),
      costs: [Mana("{2}{U}"), Tap],
      effect: [DrawDiscard(draw_count: 1, discard_count: 1, order: DrawThenDiscard)],
    ),
  ],
)
